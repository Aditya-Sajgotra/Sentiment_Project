name: Sentiment-Project

on: [push]

jobs:
  builds:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v4

      - name: install
        run: make install

      - name: lint
        run: make lint

      - name: test
        run: make test
