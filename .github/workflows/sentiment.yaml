name: Sentiment-Project

on: [push]

jobs:
  builds:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v4

      - name: install
        run: make install

      - name: lint
        env:
          PYTHONPATH: .
        run: make lint

      - name: test
        run: make test
